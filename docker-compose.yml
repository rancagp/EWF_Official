version: '3.8'

services:
  ewf-official:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=https://ewf-admin.newsmaker.id
    restart: unless-stopped
